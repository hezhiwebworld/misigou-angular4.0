import { AddnumDirective } from './addnum.directive';

describe('AddnumDirective', () => {
  it('should create an instance', () => {
    const directive = new AddnumDirective();
    expect(directive).toBeTruthy();
  });
});
